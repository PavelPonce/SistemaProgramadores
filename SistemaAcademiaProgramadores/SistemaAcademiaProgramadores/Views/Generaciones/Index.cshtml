@model IEnumerable<SistemaAcademiaProgramadores.Models.tbGeneracione>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nuevo", "Create")
</p>
<table class="table table-responsive">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Gener_Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gener_Anhio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Gener_Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gener_Anhio)
        </td>
        <td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Gener_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Gener_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Gener_Id })
        </td>
    </tr>
}

</table>
